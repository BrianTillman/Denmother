- id: study_timer_lamp
  alias: Study Timer Lamp
  mode: single
  triggers:
    - trigger: event
      event_type: timer.finished
      event_data:
        entity_id: timer.study_delay
  actions:
    - action: input_boolean.turn_on
      target:
        entity_id: input_boolean.timer_lamp
